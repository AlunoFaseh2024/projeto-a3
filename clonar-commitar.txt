cd desktop
git clone https://github.com/cleberleao/oficina-spring-boot.git
(entrar dentro da pasta baixada no terminal)
git init
git config --global user.email "email"
git config --global user.name "nome ou usuário do github"
(editar itens dentro da pasta)
git add .
git commit -m "repositorio"
